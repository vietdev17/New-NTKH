{
  "buildCommand": "npm install --workspace=apps/fe --include=dev && npm run build --workspace=apps/fe",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/fe/.next"
}
